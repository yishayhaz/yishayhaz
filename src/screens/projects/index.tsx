import { component$ } from "@builder.io/qwik";

export const ProjectsScreen = component$(() => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        minHeight: "100vh",
      }}
    >
      Projects
    </div>
  );
});
